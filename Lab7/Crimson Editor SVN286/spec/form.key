[-COMMENT-:GLOBAL]
# FORM LANGUAGE KEYWORDS SPECIFICATIONS FILE FOR CRIMSON EDITOR
# This keywords specification file for Jos Vermaseren FORM 3.0 was made by MAURO CAMBIASO.
########################################################
# Here fill in with all the known keywords/commands to be used in the program code file,
# I guess that each set of commands that you wish to appear with the same colour should
# all be on the same [KEYWORD*:Glob*].
# If it were a LaTeX keyword specification file here we should add thing like: \\ \acute \addtolength \alpha
# \and \angle \appendixname \approx \arabic  \author \backslash
# \begin \renewenvironment \TeX \dots
#################################################
# Though FORM is not casesensitive and it addmitts abbrevs. such as
# F (or f) for Function/s  I won't include the abbrevs. in the keywords
# highlighting to avoid conffussion in the code itself when Local exppression
# such as Local F1= v(i)*u(i) are used, Otherwise the local expr. F1 would
# appear highlighted.
#################################################

[KEYWORDS0:GLOBAL]
# added by igkang
 #ifndef #define #endif #else #if #ifdef #message
 #procedure #call #endprocedure
 #switch #break #case #endswitch #do #enddo

# added by igkang
if else elseif endif while endwhile label
set fill keep multiply

Symbol Symbols Function Functions
CFunction CFunctions Commuting
Tensors Tensor CTensor NTensor Vector Vectors
Indices Index
AutoDeclare

[KEYWORDS1:GLOBAL]
Local Global

[KEYWORDS2:GLOBAL]
Bracket AntiBracket Trace
contract sum ToVector ToTensor
Print repeat endrepeat

[KEYWORDS3:GLOBAL]
end .sort clear store

[KEYWORDS4:GLOBAL]
symmetric antisymmetric cyclic rcyclic

[KEYWORDS5:GLOBAL]
abs_  bernoulli_  binom:  delta_  deltap_  fac_  invfac_  max_  min_  mod_
root_  sig_  sign_  theta_  thetap_ acos_  asin_ atan_  atan2_  acosh_
asinh_  atanh_  cos_  sin_  tan_  li2_  lin_  ln_  sqrt_ i_ hbar

[KEYWORDS6:GLOBAL]
sum_ sump_

[KEYWORDS7:GLOBAL]
id identify

[KEYWORDS8:GLOBAL]
Dimension

[KEYWORDS9:GLOBAL]
Off statistics
