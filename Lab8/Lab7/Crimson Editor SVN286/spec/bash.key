[-COMMENT-:GLOBAL]
# BASH SHELL SCRIPT KEYWORDS FILE FOR CRIMSON EDITOR

# KEYWORDS0: flow control, bash commands (standard)
# KEYWORDS1: pipes, filters, and formatting

[KEYWORDS0:GLOBAL]
if then echo else elif fi for in while until do
done case esac return exit trap shift
lt gt eq function

[KEYWORDS1:GLOBAL]
awk sed tr printf cut grep egrep fgrep sort
tee mv cp rm touch make gzip tar

