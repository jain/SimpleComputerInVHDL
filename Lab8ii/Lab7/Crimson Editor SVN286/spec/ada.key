[-COMMENT-:GLOBAL]
# ADA LANGUAGE KEYWORD FILE FOR CRIMSON EDITOR

[KEYWORDS0:GLOBAL]
abort abs abstract accept aliased all and at access array
boolean begin body
case character constant
declare delay delta digits do
else elsif end entry exception exit
for function false
generic goto
if is in integer
limited loop
mod
new not null
of or others
out
package pragma
private procedure protected
return reverse
raise range record rem renames requeue real
select separate subtype
tagged task terminate then type true
until use
when while with
xor
